/*
 * PersonDetails Messages
 *
 * This contains all the text for the PersonDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PersonDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonDetails container!',
  },
});
