// import { selectPersonDetailsDomain } from '../selectors';

describe('selectPersonDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
