/*
 * MovieDetails Messages
 *
 * This contains all the text for the MovieDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MovieDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MovieDetails container!',
  },
});
