/*
 * TvDetails Messages
 *
 * This contains all the text for the TvDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TvDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TvDetails container!',
  },
});
