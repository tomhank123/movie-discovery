/*
 * PersonListPage Messages
 *
 * This contains all the text for the PersonListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PersonListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonListPage container!',
  },
});
