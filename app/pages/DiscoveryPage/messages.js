/*
 * DiscoveryPage Messages
 *
 * This contains all the text for the DiscoveryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DiscoveryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Discovery Page container!',
  },
});
